const ok = () => {
  return "OK!"
}

const reactComponent = () => {
  return (
    <span>OK!</span>
  )
}

const test = {
  ok: ok,
  reactComponent: reactComponent
}

export default test
export { test }
